services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: finpath
      POSTGRES_PASSWORD: finpath
      POSTGRES_DB: finpath
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
